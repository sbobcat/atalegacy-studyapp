const config = {
    content: [
        './index.html',
        './src/**/*.{ts,tsx}',
    ],
    theme: {
        extend: {
        // Custom design tokens can be added here
        },
    },
    plugins: [],
};
export default config;
